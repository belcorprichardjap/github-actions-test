name: Second Workflow

on: [push]

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Simple Js Action
        id: hello
        uses: ./actions/hello
        with:
          who-to-greet: 'Richard Aguirre'
      - name: Response of Simple Js Action
        run: echo "${{steps.hello.outputs.time}}"

