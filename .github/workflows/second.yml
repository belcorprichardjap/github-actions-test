name: Second Workflow

on: [push]

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
     - name : echo a string
       run: echo "Hello World"
     - name : multiline command
       run: |
            echo "Hello"
            echo "World"

        
